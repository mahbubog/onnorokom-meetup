import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.brightslothcrawl',
  appName: 'bright-sloth-crawl',
  webDir: 'dist'
};

export default config;
